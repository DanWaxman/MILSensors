[project]
name = "milsensors"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "jax==0.4.6",
    "jaxlib==0.4.6",
    "numpy<2",
    "osmnx>=2.0.6",
    "scipy==1.11.3",
    "uncertainty-toolbox>=0.1.1",
]


[tool.setuptools.packages.find]
include = ["milsensors*"] 
exclude = ["tests*", "docs*", "data*", "experiments*"]
